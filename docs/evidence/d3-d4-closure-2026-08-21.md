# D3–D4 工程收口证据

> 日期：2026-08-21  
> 里程碑结果：已满足 `Engineering-Schedule.md` 定义的 D3–D4 退出证据  
> 下一阶段：D5 定义链路（尚未开始）

## 1. D3 退出证据

| 条件 | 真实证据 | 结果 |
|---|---|---|
| Web/API 可启动 | ego-lite 访问本地工作区；设置页返回 API `ok` | 通过 |
| PostgreSQL 可连接 | 设置页真实显示 `PostgreSQL · postgresql / 在线` | 通过 |
| migration 可执行 | `pnpm db:migrate` 在 PostgreSQL 16.15 上退出码 0 | 通过 |
| 配置缺失 fail fast | 必填配置、绝对路径、独立 Roots 和可执行 Codex 路径测试通过 | 通过 |
| 基础命令退出码 0 | `pnpm check` 和 `pnpm build` 真实退出码 0 | 通过 |
| Runtime 路径 | Artifact Root、Workspace Root 已加载；Codex CLI `0.148.0-alpha.15` 只读检查通过 | 通过 |

## 2. D4 退出证据

| 条件 | 真实证据 | 结果 |
|---|---|---|
| 核心单元/集成测试 | Web 6 项、API 纯逻辑 19 项、PostgreSQL 集成 18 项通过 | 通过 |
| 重复审批不重复执行 | Gate/Permission 顺序与并发双击幂等测试通过 | 通过 |
| 任务依赖环拒绝 | 纯逻辑环检测测试通过 | 通过 |
| 双认领拒绝 | PostgreSQL 并发 ready Task 仅一个 worker 认领成功 | 通过 |
| 依赖未完成不可领取 | PostgreSQL 依赖任务测试通过 | 通过 |
| Run 恢复幂等 | 快照/恢复测试通过；未对账外部副作用时 fail closed | 通过 |
| 桌面/移动可见 | ego-lite `1440×900` 双栏；`390×844` 同屏切换群聊/产物；均无页面级滚动 | 通过 |
| 真实 API/Event 投影 | 工作区从 PostgreSQL 快照加载控制面实体，带 cursor 短轮询 | 通过（规格允许的 SSE 降级路径） |

## 3. 附件 7 项修改

1. 品牌显示为“造物工场”，前置项目已有 `/icon.svg`。
2. 工作区大项目标题已移除。
3. 第 6 用户阶段显示“开发”；冻结的“分阶段开发”和“后端 → 前端”仍保留在内部状态/事件/产物。
4. DAG 标题区不再显示 nodes/edges 计数。
5. DAG 标题下说明文案已移除；2 条边标签均为“依赖”。
6. Next.js 左下开发指示器不存在。
7. MiniMap 已投影 3 个节点，不再是空容器。

## 4. ego-lite 关键交互证据

- `@AI PM` 参与者按钮会将 `@AI PM ` 插入消息框。
- Gate “退回修改”无理由时被拦截并显示必填错误。
- Artifact 抽屉返回通过 Artifact Root 路径和 SHA-256 校验的 D4 明示样例内容，下载可用。
- 设置页显示 PostgreSQL 在线、Codex CLI 只读检查通过、Roots 已加载，DeepSeek 仍未配置。
- Console/Window/Promise 错误记录为 0。

## 5. 真实回归命令

```text
pnpm db:migrate                 exit 0
pnpm check                      exit 0
  Web Vitest                    6 passed
  API 纯逻辑 pytest          19 passed, 18 integration skipped
  ESLint / TypeScript / Ruff    passed
pnpm test:api:integration       18 passed
pnpm build                      exit 0
```

已知非阻塞警告：FastAPI TestClient 链路报 1 条 Starlette `httpx` 弃用警告；未隐藏该警告。

## 6. 尚未完成

- DeepSeek 接入渠道、模型名、Base URL、安全 SecretRef 和真模型冒烟。
- LangGraph Agent Run 图、4 个真实 Agent 运行和 D5 定义链路。
- Codex CLI Adapter 的完整 Builder 执行；当前仅 `--version` 只读 smoke。
- CopilotKit/AG-UI 真流式事件；当前是带 cursor 的短轮询降级。
- 部署、线上 URL、真实种子用户内测、BRD/G6 和反馈迭代。
- 原生截图式设计 QA：ego-lite `Page.captureScreenshot` 持续超时，打印替代图缺失文字；根目录 `design-qa.md` 依规则保持 `final result: blocked`。

## 7. 阶段裁决

D3–D4 工程里程碑按冻结排期的退出条件收口。这不代表 D5、Agent、真模型、完整视觉验收、MVP 或内测已完成。下一阶段是 D5，在 DeepSeek 非密钥配置和本地 SecretRef 安全配置完成前，不启动真模型放行。
